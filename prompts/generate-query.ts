const prompt = `Given this job description, write 3 realistic candidate general search queries in natural language that should retrieve this job. Each query should not exceed 5-6 words. Respond only with 3 lines of text and no other explanation, one line per query. Job Post:`;

export default prompt;
